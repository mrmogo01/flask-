# Используем официальный образ Python версии 3.8 в минимальной конфигурации (slim) как базовый образ
FROM python:3.8-slim
# Устанавливаем рабочую директорию в контейнере
WORKDIR /app
# Копируем файл с перечнем зависимостей в рабочую директорию
COPY requirements.txt /app/
# Устанавливаем зависимости из файла requirements.txt
RUN pip install -r requirements.txt
# Копируем все файлы из текущей директории на локальной машине в рабочую директорию контейнера
COPY . /app
# Задаем команду для запуска приложения Flask. Приложение будет доступно на всех интерфейсах контейнера
CMD ["flask", "run", "--host=0.0.0.0"]